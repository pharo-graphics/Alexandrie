SmalltalkCISpec {
  #preLoading: 'scripts/ciPreLoading.st',
  #loading : [
    SCIMetacelloLoadSpec {
      #baseline : 'Alexandrie',
      #directory : 'src',
      #platforms : [ #pharo ]
    }
  ],
  #testing : {
    // Exclude Harfbuzz tests until we have new VMs with updated libs
    #exclude : {
      #packages : [ 'Alexandrie-Harfbuzz-Tests' ]
    }
  }
}
